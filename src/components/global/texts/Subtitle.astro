---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<h3>{ text }</h3>

<style>
  h3 {
    font-family: var(--font-2);
    text-transform: uppercase;
    font-size: 16px;
    color: var(--black);
    font-weight: 500;
    opacity: .7;
  }
</style>