
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/logo.png" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>LIDER Soft</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --font-1: "Unbounded";
    --font-2: "Red Hat Display";

    --padding-inline: 15vw;

    --bg: #d4d4d4;
    --white: #FFFFFF;
    --black: #000000;
    --accent: #FFFFFF;
    --primary-50: #eff9ff;
    --primary-100: #dceffd;
    --primary-200: #c0e5fd;
    --primary-300: #95d6fb;
    --primary-400: #63bdf7;
    --primary-500: #3a9ef2;
    --primary-600: #2983e7;
    --primary-700: #2159ac;
    --primary-800: #2159ac;
    --primary-900: #204c88;
    --primary-950: #0a1a29;
    --primary-950-40: rgba(10, 26, 41, .9);
    --secondary-200: #d1d1d1;
    --secondary-300: #b0b0b0;
    --secondary-500: #787878;
    --secondary-600: #5d5d5d;
    --secondary-800: #454545;
  }

  @media screen and (max-width: 768px) {
    :root {
      --padding-inline: 8vw;
    }
  }

  * {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
    font-weight: 100;
    font-family: var(--font-2);
  }
  html {
    overflow-x: hidden;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-family: var(--font-1);
  }

  .not-landing {
    margin-top: calc(100svh - 20vh);
  }

  svg {
    width: 100%;
    height: 100%;
  }

  img {
    font-size: 0;
  }

  ::-webkit-scrollbar {
    width: 8px;
    height: 8px;
  }
  ::-webkit-scrollbar-track {
    background: var(--secondary-300);
  }
  ::-webkit-scrollbar-thumb {
    background: var(--primary-500);
  }
  
  /* CSS ANIMATIONS ON SCROLL */

  .animationNone {
    animation: none;
  }

  .appear100 {
    opacity: 0;
    animation: onlyOpacity100 linear forwards;
    animation-timeline: view();
    animation-range: 100px 350px;
  }

  @keyframes onlyOpacity100 {
    to {
      opacity: 1;
    }
  }

  .appear80 {
    opacity: 0;
    animation: onlyOpacity80 linear forwards;
    animation-timeline: view();
    animation-range: 100px 350px;
  }

  @keyframes onlyOpacity80 {
    to {
      opacity: .8;
    }
  }

  .fadeIn {
    scale: .8;
    opacity: 0;
    filter: blur(5px);
    animation: fadeInLeft linear forwards;
    animation-timeline: view();
    animation-range: 100px 350px;
  }

  @keyframes fadeInLeft {
    to {
      scale: 1;
      opacity: 1;
      filter: none;
    }
  }

  .fadeInLeft100 {
    scale: .8;
    opacity: 0;
    filter: blur(5px);
    transform: translateX(-100%);
    animation: fadeInLeft linear forwards;
    animation-timeline: view();
    animation-range: 100px 350px;
  }

  .fadeInLeft50 {
    scale: .8;
    opacity: 0;
    filter: blur(5px);
    transform: translateX(-100%);
    animation: fadeInLeft linear forwards;
    animation-timeline: view();
    animation-range: 100px 300px;
  }

  @keyframes fadeInLeft {
    to {
      scale: 1;
      opacity: 1;
      filter: none;
      transform: translateX(0);
    }
  }
</style>
